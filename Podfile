# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SampleSourceProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SampleSourceProject
  pod 'RealmSwift'
  pod 'Swinject'
  pod 'SwinjectStoryboard'
  pod 'SwiftyJSON'
  pod 'Alamofire'

  target 'SampleSourceProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SampleSourceProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
